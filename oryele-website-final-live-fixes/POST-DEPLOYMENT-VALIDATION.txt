ORYELE POST-DEPLOYMENT TECHNICAL SEO VALIDATION

Run after uploading the package and purging Cloudflare cache.

1. STATUS AND REDIRECTS
curl -I http://oryele.ai/
curl -I http://www.oryele.ai/
curl -I https://www.oryele.ai/
curl -I https://oryele.ai/

Expected: each noncanonical variant returns one 301 or 308 to https://oryele.ai/; canonical homepage returns 200.

2. CRAWL FILES
curl -I https://oryele.ai/robots.txt
curl -I https://oryele.ai/sitemap.xml

Expected: HTTP 200. sitemap.xml should use application/xml or text/xml.

3. REAL 404
curl -I https://oryele.ai/technical-seo-missing-page-928374

Expected: HTTP 404, not 200 and not a homepage redirect. Configure the server to serve 404.html while preserving status 404.

4. HEADERS
curl -I https://oryele.ai/

Confirm HSTS, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, frame protection, CSP, compression and suitable cache headers.

5. FORM
Submit one genuine request at https://oryele.ai/contact.html. Confirm FormSubmit accepts it, redirects to ?submitted=1, and delivers to info@oryele.com. Never display success unless the provider accepted the form.

6. GOOGLE
Submit https://oryele.ai/sitemap.xml in Google Search Console. Inspect the homepage, platform, solutions, pricing, contact, privacy and terms pages and request indexing.

7. VALIDATORS
Run Google Rich Results Test, Schema.org Validator, LinkedIn Post Inspector, Bing Webmaster Tools and PageSpeed Insights for mobile and desktop.

8. CLOUDFLARE BOTS
Confirm no WAF, Bot Fight Mode, managed challenge or rate-limit rule blocks Googlebot, Bingbot, OAI-SearchBot, GPTBot, ClaudeBot or PerplexityBot.
